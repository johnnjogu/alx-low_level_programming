#include<stdio.h>
/**
 * main - entry point
 *
 * Return: Always return 0
 */
int main(void)
{
char a;
for (a = 'a'; a <= 'z'; a++)
putchar(0)
for (a = 'A'; a <= 'Z'; a++)
putchar(a);
putchar ('\n');
return (0);
}
