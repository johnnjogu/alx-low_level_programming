#include <stdio.h>
#include <stdlib.h>

/**
 * main - entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int numb;

	for (numb = 0; numb <= 9; numb++)
	{
		printf("%d", numb);
	}

	printf("\n");
	return (0);
}
